version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: udagram-reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: sudkul/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: sudkul/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: sudkul/udagram-frontend